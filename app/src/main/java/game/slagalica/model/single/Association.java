package game.slagalica.model.single;

public class Association extends Game {

    public Association() {
    }
    public Association(int rounds, int maxPoints, int minPoints, int duration, int activeRound) {
        super(rounds, maxPoints, minPoints,activeRound , duration);
    }
}
